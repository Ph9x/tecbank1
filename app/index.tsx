import { View, Text, StyleSheet } from "react-native";

function App() {
    return (
        <View>
            <Text>PEdro</Text>
        </View>
    );
}

const style = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "#00f"
    }
});